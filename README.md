# 🍽️ Restaurant Manager — Angular + Tailwind

**Полнофункциональная POS-система для ресторанов** на **Angular 18** (Standalone Components) + **Tailwind CSS**.

![Angular](https://img.shields.io/badge/Angular-18-red)
![Tailwind](https://img.shields.io/badge/TailwindCSS-3.4-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-5.4-blue)

## 🎯 Особенности

- ✅ **Управление столиками** с визуальными статусами
- ✅ **Создание и редактирование заказов** в реальном времени
- ✅ **Каталог меню** с категориями и поиском
- ✅ **Система оплаты** (наличные, карта, QR, частичная)
- ✅ **Авторизация** с защищенными роутами
- ✅ **Reactive архитектура** с RxJS
- ✅ **Адаптивный дизайн** для планшетов
- ✅ **Lazy Loading** всех модулей

## 📁 Архитектура проекта

```
Restaurant-Manager_Angular-frontend/
├── src/
│   ├── app/
│   │   ├── core/                          # Ядро приложения
│   │   │   ├── models/
│   │   │   │   └── restaurant.models.ts   # Модели данных (Table, Order, MenuItem)
│   │   │   ├── services/
│   │   │   │   ├── auth.service.ts        # Авторизация
│   │   │   │   ├── table.service.ts       # Управление столиками
│   │   │   │   ├── order.service.ts       # Управление заказами
│   │   │   │   ├── menu.service.ts        # Работа с меню
│   │   │   │   └── payment.service.ts     # Обработка оплаты
│   │   │   └── guards/
│   │   │       └── auth.guard.ts          # Защита роутов
│   │   ├── features/                      # Функциональные модули
│   │   │   ├── login/                     # Страница входа
│   │   │   ├── tables/                    # Список столиков
│   │   │   ├── order-detail/              # Работа с заказом
│   │   │   ├── menu/                      # Выбор блюд
│   │   │   └── payment/                   # Оплата заказа
│   │   ├── layouts/
│   │   │   └── main-layout/               # Основной layout с навигацией
│   │   ├── shared/
│   │   │   └── components/                # Переиспользуемые компоненты
│   │   ├── app.component.ts
│   │   ├── app.config.ts
│   │   └── app.routes.ts                  # Роутинг
│   ├── assets/
│   ├── index.html
│   ├── main.ts
│   └── styles.css
├── angular.json
├── package.json
├── tailwind.config.cjs
└── tsconfig.json
```

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
npm install
```

### 2. Запустите проект

```bash
npm start
```

или

```bash
ng serve
```

### 3. Откройте браузер

Перейдите по адресу [http://localhost:4200](http://localhost:4200)

## ✨ Возможности

- ✅ **Angular 18** с Standalone Components
- ✅ **Tailwind CSS** для стилизации
- ✅ **Lazy Loading** компонентов
- ✅ **Роутинг** с несколькими страницами
- ✅ **HTTP Client** для API запросов
- ✅ Адаптивный дизайн
- ✅ Современная архитектура (Feature-based)

## 🔄 Флоу работы официанта

### 1️⃣ Выбор столика (`/tables`)
- Официант видит все столики с их статусами:
  - 🟢 **Свободен** — кликнуть для создания заказа
  - 🔵 **Занят** — кликнуть чтобы открыть заказ
  - 🟡 **Ожидает оплаты** — перейти к оплате
  - ⚫ **Закрыт** — недоступен

### 2️⃣ Создание заказа (`/orders/new`) — ТОЛЬКО для свободных столиков
- Экран подтверждения создания заказа:
  - Номер столика
  - Выбор количества гостей (1-20)
  - Быстрые кнопки (1, 2, 4, 6, 8 гостей)
  - Две опции:
    - **"Создать заказ"** — создать пустой заказ
    - **"Создать и выбрать блюда"** — сразу перейти в меню
  - Кнопка "Отмена" — вернуться к столикам

### 3️⃣ Работа с заказом (`/orders/:tableId`)
- Просмотр текущих позиций заказа
- Изменение количества блюд
- Добавление новых позиций (переход в меню)
- Удаление позиций
- Закрытие заказа для оплаты

### 4️⃣ Выбор блюд (`/menu`)
- Фильтрация по категориям (Закуски, Супы, Горячие, и т.д.)
- Поиск по названию
- Добавление в корзину с выбором количества
- Подтверждение выбора → возврат к заказу

### 5️⃣ Оплата (`/payment/:orderId`)
- Выбор способа оплаты:
  - 💵 Наличные (с расчетом сдачи)
  - 💳 Карта
  - 📱 QR-код
  - 💰 Частичная (наличные + карта)
- Проведение оплаты
- Освобождение столика

## 📄 Основные сущности

### Table (Столик)
```typescript
{
  id: number;
  number: string;           // номер столика
  status: TableStatus;      // free | occupied | waiting_payment | closed
  activeOrderId: number | null;
  seats: number;            // количество мест
}
```

### Order (Заказ)
```typescript
{
  id: number;
  tableId: number;
  items: OrderItem[];       // позиции заказа
  status: OrderStatus;      // new | in_progress | served | waiting_payment | paid
  totalAmount: number;
  waiterName: string;
}
```

### MenuItem (Позиция меню)
```typescript
{
  id: number;
  name: string;
  category: MenuCategory;   // Закуски, Супы, Горячие, и т.д.
  price: number;
  available: boolean;
  preparationTime: number;  // время приготовления в минутах
}
```

## 🛠 Технологии

- **Angular 18** - фреймворк
- **TypeScript** - язык программирования
- **Tailwind CSS** - CSS-фреймворк
- **RxJS** - реактивное программирование
- **Angular Router** - маршрутизация

## 📝 Скрипты

- `npm start` - запуск dev-сервера
- `npm run build` - сборка проекта
- `npm run watch` - сборка с отслеживанием изменений
- `npm test` - запуск тестов

## 🎨 Кастомизация

### Изменение темы Tailwind
Отредактируйте `tailwind.config.cjs`:

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#your-color',
      }
    },
  },
}
```

### Добавление новых компонентов
Используйте Angular CLI:

```bash
ng generate component features/your-feature
```

## 📚 Структура кода

### Core
Содержит базовые сервисы (API, Auth и т.д.)

### Features
Бизнес-логика и страницы приложения

### Shared
Переиспользуемые компоненты и утилиты

### Layouts
Общие макеты страниц

## 🤝 Вклад

Приветствуются Pull Request'ы и Issue!

## 📄 Лицензия

MIT License

